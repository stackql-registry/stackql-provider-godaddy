# python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# IDE / editor settings
.vscode/
.idea/
*.swp
*.swo

# misc
.DS_Store
*.log
*.tmp
*.bak

# stackql
.stackql/
stackql
stackql.exe
stackql-zip
stackql-server.log
nohup.out

# credentials (the smoke suite sources .env)
.env
**/.env
**/.env.*.local

# node
**/node_modules/

# build outputs
**/build/
**/.docusaurus/
**/.cache/

# integration test registry copy (materialised per run)
tests/integration/.registry-tmp/

# vendored shared docusaurus config (cloned at build time)
website/.shared-config/

# generated docs are committed (git history date-stamps every page);
# the website lock file is kept for reproducible builds
